<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.plan.PlanDAOInter">	
	<!-- 전체 목록 -->
	<select id="list_all" resultType="dev.mvc.plan.PlanVO">
		SELECT planID, pname, pdescription, heart
		FROM Plan
		ORDER BY planID DESC
	</select>
	<!-- 조회 -->
	<select id="read" resultType="dev.mvc.plan.PlanVO" parameterType="int">
		SELECT planID, pname, pdescription, heart
		FROM Plan
		WHERE planID=#{planID}
	</select>
</mapper>