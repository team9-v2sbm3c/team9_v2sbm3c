<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.guest.GuestDAOInter">	
  <select id="checkID" resultType="int" parameterType="String">
    SELECT COUNT(guestID) as cnt
    FROM guest
    WHERE guestID=#{guestID}
  </select>
	<!-- 조회 -->
	<insert id="create" parameterType="dev.mvc.guest.GuestVO" >
    INSERT INTO GUEST(guestno, guestID, gpassword, gname, gemail)
    VALUES (guest_seq.nextval, #{guestID}, #{gpassword}, #{gname}, #{gemail});
	</insert>
  
</mapper>